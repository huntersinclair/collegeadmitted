"""
Basic test file to ensure pytest is properly set up.
"""

def test_placeholder():
    """
    A placeholder test that always passes.
    This ensures that pytest has at least one test to run
    and prevents CI pipeline failures due to missing tests.
    """
    assert True 